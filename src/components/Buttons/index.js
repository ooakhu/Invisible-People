import React from 'react';
import {DonateButton, MemberButton, ApplyButton} from "./style";


export const DonateBtn = () => {
    return (
        <DonateButton>DONATE</DonateButton>

    );
};

export const MemberBtn = () => {
    return (
        <MemberButton>VOLUNTEER</MemberButton>
    );

};


export const ApplyBtn = () => {
    return (
        <ApplyButton>Apply Today</ApplyButton>
    );
};
